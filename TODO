1. Allow only valid function names for kernel
2. Compile only if not existing .so file
3. Allow extra compile options
4. Define function parameters automatically
5. Create function to catch errors after kernel launch
 
